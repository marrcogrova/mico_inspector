  version: 2.1
  jobs:
    build:
      docker: 
        - image: circleci/node:4.8.2 # the primary container, where your job's commands are run
      steps:
        - checkout # check out the code in the project directory
        - run: bash script/install_dependencies.sh
        - run: bash script/install_librealsense2.sh
        - run: mkdir build
        - run: cd build
        - run: cmake..
        - run: make -j4